<%= content_tag :h1, 'MainPage' %>

<%= bootstrap_form_tag(url: search_main_page_index_path, method: :get) do |f| %>
  <%= f.text_field :vin %>
  <%= f.submit 'Find car in ledger' %>
<% end %>

<%= content_tag :br %>

<%= @responses %>

<%#= link_to 'Add car', new_main_page_path, class: "btn btn-info" %>
